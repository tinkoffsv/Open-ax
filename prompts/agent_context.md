# Architectural decisions (OpenAX)

Task: {{task}}

These decisions were recorded by the developer. They state the intent behind the project's structure.

- Use the ones that concern your task: the same capability, component, data, or kind of mechanism. Ignore the rest.
- Follow them. If your task seems to require contradicting one, stop and ask the developer before proceeding.
- Don't introduce a second mechanism for something a decision already assigns to a specific mechanism.

{{decisions}}
